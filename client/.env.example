# MongoDB connection string (MongoDB Atlas recommended for production)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/thailandKitchen

# API base URL — use /api when frontend and API deploy together on Vercel
NEXT_PUBLIC_API_URL=/api
